# Plant-Disease-Detection
A complete deep-learning system for detecting multiple plant diseases from leaf images using CNNs. The model classifies each leaf into its disease category (or healthy) and also estimates disease severity by segmenting infected regions and calculating the infection ratio.
